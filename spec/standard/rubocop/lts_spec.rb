# frozen_string_literal: true

RSpec.describe Standard::Rubocop::Lts do
  it "has a version number" do
    expect(Standard::Rubocop::Lts::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
